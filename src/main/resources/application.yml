spring:
  datasource:
    url: jdbc:mysql://localhost:3306/livecoding_arena?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Birunda_127
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  
  thymeleaf:
    cache: false
  
  security:
    enabled: false

server:
  port: 8088

jwt:
  secret: mySecretKey123456789012345678901234567890
  expiration: 86400000

logging:
  level:
    com.livecoding.arena: DEBUG